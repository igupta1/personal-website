# HTTP client with async support
httpx>=0.27.0

# Fuzzy string matching (faster than fuzzywuzzy)
rapidfuzz>=3.6.0

# Data processing
pandas>=2.0.0

# HTML parsing (for fallback scraping)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Browser automation (fallback only, already in workflow)
playwright>=1.40.0

# Environment variables
python-dotenv>=1.0.0

# Google Gemini AI (decision maker lookup with Search grounding)
google-genai>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
