# SMB Marketing Lead Finder - Dependencies

# Web scraping (for main.py only)
playwright>=1.40.0
requests>=2.31.0
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
html2text>=2020.1.16

# Data processing
pandas>=2.0.0

# AI/LLM (Gemini for contact enrichment, GPT for icebreakers)
google-genai>=1.55.0
google-generativeai>=0.3.0
openai>=1.12.0

# Environment variables
python-dotenv>=1.0.0

# Note: process_demo_leads.py does NOT need playwright - it only uses Gemini + GPT
