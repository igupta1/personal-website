# Copy to pipeline/.env and fill in real values. Never commit .env.

# LLM keys
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Job board APIs (free tier, optional)
ADZUNA_APP_ID=
ADZUNA_APP_KEY=

# Vercel Blob (used by api/upload-leads.js, not the pipeline directly,
# but kept here for local-test parity)
BLOB_READ_WRITE_TOKEN=
